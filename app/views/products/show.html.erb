<section class="product">

	<h2><%= @product.name %></h2>
	<%= image_tag @product.image.url(:medium) %>
	<p><%= @product.description %></p>
	<p><small><%= number_to_currency @product.price_in_pence.to_f/100, unit: "£", precision: 2 %></small></p>

	<p><%= link_to "Buy", new_product_order_path(@product), data: {turbolinks_track: false} %></p>

	<p><small>For more info contact <%= mail_to @product.user.email %></small></p>
</section>


